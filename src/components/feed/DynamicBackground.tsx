/**
 * No-op placeholder — background colors are now handled by ZoneWrapper's
 * CSS-only blurred color clouds that scroll with content.
 * Kept as a component so imports don't break.
 */
export function DynamicBackground() {
  return null;
}
